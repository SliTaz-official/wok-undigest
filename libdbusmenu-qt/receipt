# SliTaz package receipt.

PACKAGE="libdbusmenu-qt"
VERSION="0.8.0"
CATEGORY="system-tools"
SHORT_DESC="Library to import and export QMenu instances using the DBusMenu."
MAINTAINER="pankso@slitaz.org"
DEPENDS="qt4"
BUILD_DEPENDS="cmake qmake"
TARBALL="$PACKAGE-$VERSION.tar.bz2"
WEB_SITE="https://launchpad.net/libdbusmenu-qt"
WGET_URL="http://launchpad.net/libdbusmenu-qt/trunk/$VERSION/+download/$TARBALL"

# Rules to configure and make the package.
compile_rules()
{
	mkdir -p $src/build
	cd $src/build
	cmake \
		-DCMAKE_BUILD_TYPE=Release \
		-DCMAKE_INSTALL_PREFIX=/usr .. &&
	make && make install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/lib
	cp -a $_pkg/usr/lib/*.so* $fs/usr/lib
}
