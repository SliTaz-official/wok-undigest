# SliTaz package receipt.

PACKAGE="python-llfuse"
SOURCE="llfuse"
VERSION="0.38"
CATEGORY="developmemnt"
SHORT_DESC="Python bindings for the low level FUSE API."
MAINTAINER="pascal.bellard@slitaz.org"
TARBALL="$SOURCE-$VERSION.tar.bz2"
WEB_SITE="http://code.google.com/p/$PACKAGE/"
WGET_URL="http://$PACKAGE.googlecode.com/files/$TARBALL"
http://python-llfuse.googlecode.com/files/llfuse-0.38.tar.bz2

DEPENDS="fuse"
BUILD_DEPENDS="python-dev fuse-dev"

# Rules to configure and make the package.
compile_rules()
{
	cd $src
	python setup.py build &&
	python setup.py test &&
	python setup.py install --root=$DESTDIR
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	cp -a $install/usr $fs
}
