# SliTaz package receipt

PACKAGE="weechat"
VERSION="0.3.2"
CATEGORY="network"
SHORT_DESC="a fast, light and extensible chat client."
MAINTAINER="rocky@slitaz.org"
DEPENDS="ncursesw libgcrypt libgnutls ncursesw libtasn1 python perl ruby"
BUILD_DEPENDS="ncurses ncurses-dev libgcrypt-dev gnutls-dev libtasn1-dev perl python python-dev ruby ruby-dev slitaz-toolchain"
TARBALL="$PACKAGE-$VERSION.tar.gz"
WEB_SITE="http://weechat.org/"
WGET_URL="http://weechat.org/files/src/$TARBALL"
TAGS="irc"

# Rules to configure and make the package.
compile_rules()
{	
	cd $src
	./configure \
		--prefix=/usr \
		--enable-ncurses \
		--enable-gcrypt \
		--enable-gnutls \
		--enable-python \
		--enable-perl \
		--enable-ruby \
		--disable-doc \
		--with-debug=0 \
		$CONFIGURE_ARGS &&
	make &&
	make DESTDIR=$PWD/_pkg install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr
	cp -a $_pkg/usr/bin $fs/usr
	cp -a $_pkg/usr/lib $fs/usr
	rm -rf $fs/usr/lib/pkgconfig
	rm -f $fs/usr/lib/weechat/plugins/*.a
	rm -f $fs/usr/lib/weechat/plugins/*.la
}
